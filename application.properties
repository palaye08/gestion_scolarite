spring.application.name=etudiant

# URL de la base de données Neon PostgreSQL
spring.datasource.url=jdbc:postgresql://ep-billowing-snow-adoht92a-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Nom d'utilisateur et mot de passe Neon
spring.datasource.username=neondb_owner
spring.datasource.password=npg_PzicjA3OWuK5

# Configuration du port
server.port=8083

# Driver PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuration SSL pour Neon (recommandé)
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5

# Personnalisation du titre, description et version de l'API
springdoc.api-docs.title=API de Gestion des Etudiants
springdoc.api-docs.description=Cette API permet de gérer les étudiants dans une école.
springdoc.api-docs.version=OPENAPI_3_1

# Activer Swagger UI
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.title=Swagger UI - Gestion des Etudiants
springdoc.swagger-ui.path=/swagger-ui.html

# Chemin par défaut pour les documents OpenAPI
springdoc.api-docs.path=/api-docs

